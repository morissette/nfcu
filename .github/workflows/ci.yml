name: CI

on:
  pull_request:
    branches:
      - master

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      checks: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: |
          pip install pipenv
          pipenv --python python install --dev
          pipenv run pip install -e .
      - uses: reviewdog/action-setup@v1
        with:
          reviewdog_version: latest
      - name: pylint
        env:
          REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          pipenv run pylint --output-format=parseable nfcu/ | \
            reviewdog -f=pylint -name=pylint -reporter=github-pr-review -fail-level=error
      - name: pycodestyle
        env:
          REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          pipenv run pycodestyle nfcu/ | \
            reviewdog -f=pycodestyle -name=pycodestyle -reporter=github-pr-review -fail-level=any

  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          pip install pipenv
          pipenv --python python install --dev
          pipenv run pip install -e .
      - name: pytest
        run: pipenv run pytest test/ -v
