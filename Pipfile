[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests = ">=2.32.0"

[dev-packages]
coverage = ">=7.0"
coveralls = ">=3.3"
pycodestyle = ">=2.11"
pylint = ">=3.0"
pytest = ">=8.0"

[requires]
python_version = "3.10"
